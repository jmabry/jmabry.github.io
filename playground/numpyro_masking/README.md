# Numpyro Masking Example

This folder contains a minimal numpyro poetry environment, to install run `poetry install --no-root`. 

Start a shell with `poetry shell`

Then open the notebook with `jupyter lab` 